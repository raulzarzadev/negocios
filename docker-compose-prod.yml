version: "3.8"

services:
  front:
    restart: always
    build:
      context: ./front-negocios
      dockerfile: Dockerfile.prod
    ports:
      - "4000:80"
    networks:
      - negocios-net
    environment:
      - REACT_APP_SIGNUP_SERVICE
      - REACT_APP_ADVERTS
      - REACT_APP_UPLOADIMAGE_SERVICE

  back:
    restart: always
    build:
      context: ./back-negocios
      dockerfile: Dockerfile.prod
    ports:
      - "4001:4001"
    networks:
      - negocios-net
    environment:
      - PORT
      - MONGO_NDB
      - JWT_SECRET_TEXT
      - EMAIL_PASSWORD
      - NDB_EMAIL
      - ENVIROMENT

  uploadimage:
    image: raulzarza/uploadimage:latest
    ports:
      - 4042:4042
    networks:
      - negocios-net
    environment:
      - ENVIROMENT
      - MONGODB_URI
      - CLOUDINARY_CLOUD_NAME
      - CLOUDINARY_API_KEY
      - CLOUDINARY_API_SECRET
      - UPLOAD_IMAGE_PORT

  signupservice:
    image: raulzarza/signupservice
    ports:
      - 3015:3015
    networks:
      - negocios-net
    environment:
      - USERS_SERVICE_DB
      - FRONT_HOST
      - JWT_SECRET_TEXT
      - SIGNUP_PORT
      - EMAIL_PASSWORD
      - EMAIL_USER

networks:
  negocios-net:
    driver: bridge
